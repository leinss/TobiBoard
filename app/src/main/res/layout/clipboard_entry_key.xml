<?xml version="1.0" encoding="utf-8"?>
<!--
    SPDX-License-Identifier: GPL-3.0-only
-->
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

        <ImageView
                android:id="@+id/clipboard_entry_pinned_icon"
                android:contentDescription="@string/clipboard_history_pinned_first"
                android:layout_width="@dimen/config_clipboard_pinned_icon_size"
                android:layout_height="@dimen/config_clipboard_pinned_icon_size"
                android:layout_marginVertical="8dp"
                android:layout_marginStart="4dp"/>

        <!-- Provide audio and haptic feedback by ourselves based on the keyboard settings.
             We just need to ignore the system's audio and haptic feedback settings. -->
        <TextView
                android:id="@+id/clipboard_entry_content"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="2dp"
                android:layout_marginHorizontal="8dp"
                android:hapticFeedbackEnabled="false"
                android:soundEffectsEnabled="false"
                android:ellipsize="end"
                android:maxLines="4"/>

    </LinearLayout>

    <!-- Annotation row — shown only when annotation is set -->
    <TextView
            android:id="@+id/clipboard_entry_annotation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="8dp"
            android:layout_marginBottom="2dp"
            android:textStyle="italic"
            android:textSize="11sp"
            android:visibility="gone"/>

    <!-- Metadata row: timestamp on left, use count on right -->
    <LinearLayout
            android:id="@+id/clipboard_entry_meta_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginHorizontal="8dp"
            android:layout_marginBottom="4dp"
            android:visibility="gone">

        <TextView
                android:id="@+id/clipboard_entry_timestamp"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="10sp"
                android:alpha="0.6"/>

        <TextView
                android:id="@+id/clipboard_entry_use_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="10sp"
                android:alpha="0.6"
                android:visibility="gone"/>

    </LinearLayout>

</LinearLayout>
