<?xml version="1.0" encoding="utf-8"?>
<!--
  All app traffic must use TLS. The keyboard only ever talks to OpenRouter,
  and it is always HTTPS - no cleartext is acceptable.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
