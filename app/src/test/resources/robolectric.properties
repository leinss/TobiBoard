# Robolectric defaults its emulated SDK to the app's targetSdk. Robolectric refuses to
# create a sandbox for Android SDK 36 unless Gradle runs on JDK 21, and this project
# compiles against JDK 17, so pin the emulated SDK to 35. This only affects the JVM test
# sandbox; the shipped app still targets SDK 36.
sdk=35
