*.iml
.idea
.gradle
.kotlin
local.properties
.DS_Store
Gemfile
build
app/build
app/release
artifacts/
app/.cxx
app/.attach_*
fastlane/Appfile
tools/*.txt
*.patch
*.diff
*.sh

# Signing keys — never commit
release-signing/
*.jks
*.keystore
*.p12
*.pkcs12
keystore.properties
*service-account*.json
play-*.json

# Local env (Keychain references + paths) — never commit
.envrc

# Native libs pulled by `make fetch-native-libs` — too large for git.
app/libs/*.aar
app/libs/*.jar

# Internal dev docs
docs/superpowers/

# Claude config
CLAUDE.md
.env

# fastlane generated
fastlane/README.md
fastlane/report.xml
